# راهنمای قدم‌به‌قدم فعال‌سازی «ورود با گوگل» 🟢

> زمان تقریبی: **۱۰ دقیقه** — به هیچ دانش فنی نیاز ندارید.
> تنها چیزی که لازم دارید: **یک حساب جیمیل**.

---

## خلاصه کار

شما در «کنسول گوگل» یک **شناسه ورود (Client ID)** می‌سازید و آن را برای من می‌فرستید.
من همه‌چیز را در سایت و سرور تنظیم و منتشر می‌کنم؛ پس از آن دکمهٔ «ورود با گوگل»
به‌صورت خودکار در صفحات ورود و ثبت‌نام فعال می‌شود.

---

## مرحله ۱ — ورود به کنسول گوگل

1. مرورگر را باز کنید و به آدرس زیر بروید:
   ```
   https://console.cloud.google.com/
   ```
2. با جیمیل خود وارد شوید (اگر قبلاً وارد شده‌اید، مرحله بعد).

## مرحله ۲ — ساخت پروژه

1. در نوار بالا، روی **نام پروژهٔ فعلی** (کنار «Google Cloud») بزنید.
2. دکمهٔ **New Project / پروژه جدید** را بزنید.
3. نام پروژه را بگذارید: `GameNet`
4. روی **Create / ایجاد** بزنید و ۳۰ ثانیه صبر کنید تا ساخته شود.
5. مطمئن شوید پروژهٔ GameNet انتخاب شده است (در نوار بالا).

## مرحله ۳ — صفحهٔ رضایت (OAuth consent screen)

1. از منوی سمت چپ: **APIs & Services / APIها و سرویس‌ها** را بزنید.
2. سمت چپ: **OAuth consent screen**.
3. گزینهٔ **External** را انتخاب کنید → **Create / ایجاد**.
4. فرم را پر کنید:
   - **App name**: `GameNet`
   - **User support email**: ایمیل جیمیل خودتان را انتخاب کنید
   - **Developer contact information**: همان جیمیل خودتان را بنویسید
5. **Save and Continue** بزنید.
6. مرحلهٔ **Scopes** — تغییری ندهید، فقط **Save and Continue**.
7. مرحلهٔ **Test users** — خیلی مهم:
   - دکمهٔ **Add users** را بزنید
   - جیمیل خودتان (و هر جیمیل دیگری که می‌خواهد وارد سایت شود) را اضافه کنید
   - **Save and Continue**
   > ⚠️ تا زمانی که اپ «آزمایشی» است، **فقط جیمیل‌هایی که اینجا اضافه شده باشند**
   > می‌توانند با گوگل وارد شوند. (بعداً اگر بخواهید همه بتوانند، کافی است
   > دکمهٔ **Publish app** را در همین صفحه بزنید.)

## مرحله ۴ — ساخت شناسهٔ ورود (Client ID)

1. از منوی سمت چپ: **Credentials / احراز هویت**.
2. بالای صفحه: **Create Credentials → OAuth client ID**.
3. **Application type**: گزینهٔ **Web application** را بزنید.
4. **Name**: دلخواه، مثلاً `GameNet-Web`.
5. بخش **Authorized JavaScript origins** ← مهم‌ترین بخش — دکمهٔ **Add URI** را بزنید
   و **این ۴ آدرس** را دقیقاً مثل زیر اضافه کنید (بدون هیچ تغییری):
   ```
   https://abadeh777iu-jpg.github.io
   https://gamenet-server.abadeh-gamenet.workers.dev
   https://gamenet-platform.pages.dev
   http://localhost:3000
   ```
6. بخش **Authorized redirect URIs** را **خالی** بگذارید (لازم نیست).
7. **Create / ایجاد** را بزنید.
8. پنجره‌ای باز می‌شود و یک **Client ID** نشان می‌دهد — چیزی شبیه:
   ```
   1234567890-abcdefghijklmnop.apps.googleusercontent.com
   ```
   روی آیکن کپی 📋 بزنید.

## مرحله ۵ — فرستادن شناسه به من ✅

شناسه کپی‌شده را همین‌جا برای من بفرستید.
من کارهای زیر را **خودم** انجام می‌دهم:

- قرار دادن شناسه در فایل پیکربندی سایت
- فعال‌سازی در سرور (env گوگل)
- انتشار نسخهٔ جدید و تست کامل
- تأیید اینکه دکمهٔ «ورود با گوگل» در ایران بدون فیلترشکن کار می‌کند

---

## سؤالات متداول

**آیا شناسهٔ Client ID یک «رمز مخفی» است؟**
خیر — این شناسه ذاتاً عمومی است و کد سایت هم آن را نمایش می‌دهد؛ امنیت اصلی
در همان لیست «آدرس‌های مجاز» و تأیید توکن توسط سرور است.

**کاربران عادی چه‌کار کنند؟**
هیچ‌کاری — پس از انتشار، در صفحهٔ ورود، دکمهٔ «ورود با گوگل» را می‌زنند و تمام.

**اگر جیمیل جدیدی خواست وارد شود چه؟**
در مرحلهٔ ۳ همان جیمیل را به فهرست Test users اضافه کنید (یا Publish بزنید).
